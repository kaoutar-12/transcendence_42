from django.urls import path
from . import views

app_name = 'game'

urlpatterns = [
    # path('queue/join/', views.join_queue, name='join_queue'),
    # path('queue/leave/', views.leave_queue, name='leave_queue'),
    # path('match/create/', views.create_match, name='create_match'),
    # path('history/', views.get_game_history, name='game_history'),
    # path('active/', views.get_active_game, name='get_active_game'),
    # path('<int:game_id>/info/', views.get_game_info, name='get_game_info'),
    # path('<int:game_id>/state/', views.get_game_state, name='get_game_state'),
]