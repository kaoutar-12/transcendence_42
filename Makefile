# to do

# build & run the application 
all:

# build & run the database
db:

# build & run the backend:
backend:

# build & run the frontend:
frontend:

# build & run the ngnix:
ngnix:

# stop application
stop:

# stop & clean
clean:

# stop, clean & all
re:
